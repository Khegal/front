import { ThemeComponentProps } from "@chakra-ui/theme";
const Button = {
	baseStyle: {
		borderRadius: 24,
	},
	variants: {
		solid: (props: ThemeComponentProps) => ({}),
	},
};

export default Button;
